"use strict";
let firstpersonName = "SHAZIA";
console.log(firstpersonName.toLowerCase());
let secondpersonName = "shazia";
console.log(secondpersonName.toUpperCase());
let thirdpersonName = "shazia nasir";
console.log(thirdpersonName.replace(/\b\w/g, (chario) => chario.toUpperCase()));
